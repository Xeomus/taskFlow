# TaskFlow

TaskFlow is a React and TypeScript frontend for managing projects and tasks.

## Development

```bash
npm install
npm run dev
```

Run `npm run lint` and `npm run build` before submitting changes.
